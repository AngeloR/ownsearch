{
  "name": "@surface/indexer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -w -p tsconfig.json",
    "start": "node dist/index.js",
    "migrate": "pnpm run build && node dist/migrate.js",
    "test": "echo \"No tests yet\""
  },
  "dependencies": {
    "ioredis": "^5.8.2",
    "knex": "^3.1.0",
    "pg": "^8.13.1"
  }
}
